# prompts/not-executed/

Prompts deliberately decided against, with no replacement.

Retire a prompt here by prepending a `RETIRED YYYY-MM-DD: <reason>` header and `git mv`ing it in. Never
silently delete a prompt; retiring preserves the record and the reason it was not run.
