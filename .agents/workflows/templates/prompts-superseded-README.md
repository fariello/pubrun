# prompts/superseded/

Prompts replaced by a better or subsequent prompt, kept for the record.

Retire a prompt here by prepending a `RETIRED YYYY-MM-DD: <reason>; superseded by <path/commit>` header
and `git mv`ing it in. Never silently delete a prompt.
