# prompts/executed/

Prompts that have been run and whose results have been filed (terminal; `done/` is an accepted alias).

The durable RESULTS of a run belong under `.agents/docs/research/<topic>/`, not here; this bucket keeps
the prompt itself for provenance. Move a prompt here only after it was actually run.
