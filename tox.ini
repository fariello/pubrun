[tox]
envlist = py38, py39, py310, py311, py312, py313, py314
isolated_build = True

[testenv]
description = Run PyTest under the {envname} environment
deps =
    pytest>=7.0
commands =
    pytest tests/ -v
