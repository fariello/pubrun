# Expected Methods Text

Below is the expected output when running `pubrun methods` on the run directory produced by `analysis.py`.

```markdown
### Computational Methods

Computational experiments were executed on a machine running Linux equipped with an AMD EPYC Processor and 32 GB of RAM. The execution environment relied on Python 3.11.8 (CPython). 

No high-level data science libraries (such as NumPy, Pandas, or Scikit-Learn) were loaded in the runtime environment.

To guarantee computational reproducibility, the exact state of the source code was archived at Git commit `b6f0075b2e23bd9589d5b7f24367e48fe3e1f27b` (repository: https://github.com/fariello/pubrun). Environment and execution provenance were tracked using the `pubrun` library [1].

**References:**
[1] Fariello, G. (2026). pubrun: A zero-dependency Python library for execution provenance and telemetry capture. GitHub repository, https://github.com/fariello/pubrun
```
