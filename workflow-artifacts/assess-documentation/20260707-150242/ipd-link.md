# IPD link

- IPD: `.agents/plans/pending/2026-07-07-assess-documentation.md`
- Summary: Fix README/CLI documentation drift after the 1.4.0 command & feature changes —
  remove the nonexistent `bug-report` command, correct the command count (20, not
  fourteen/thirteen), rename `report`→`show` and `resources`→`res`/`cpu`/`mem` in the README,
  purge already-shipped items (`combined`, timestamped capture, CI) from the Roadmap "Future",
  add missing README CLI entries (init/self-check/inspect/bench), consolidate the duplicate
  License and citation sections, and align the CHANGELOG nav. 14 findings (5 High, 6 Medium,
  2 Low, 1 no-op); all documentation-only, Low Remediation Risk.
