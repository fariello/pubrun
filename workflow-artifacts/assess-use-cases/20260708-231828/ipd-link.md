# IPD link

- IPD: `.agents/plans/pending/2026-07-08-assess-use-cases.md`
- Summary: Use-case coverage is **strong**; the IPD proposes 6 ordered, low-risk changes to close
  narrow scenario gaps - fix the broken documented `resources` alias (U1/U2), test `pubrun init`
  (U3), test SIGHUP + real-SIGKILL crashed classification (U4), add an end-to-end HPC
  parent-child hydration test wiring in the dead `hpc_node.py` fixture (U5), test two concurrent
  live runs in one output dir (U6), and correct the stale "example should be added"
  research-use.md text (U7). Nothing deferred (all Remediation Risk Low to Low-Medium).
